# src/main/scala

The primary Scala/Chisel workspace.

- `device`: Peripheral and IO device implementations.
- `system`: Top-level system integrations.
- `top`: Top-level module definitions for different configurations.
- `utils`: Utility classes and helper functions.
- `xiangshan`: Core processor implementation (frontend, backend, cache, etc.).
