# L2Top.scala

`L2Top` manages the Level 2 (L2) Cache and its integration with the core and external buses.

## Overview
It acts as the bridge between the core's L1 caches and the outer memory system.

## Key Features
- High-performance L2 Cache implementation.
- Support for multiple memory protocols (TileLink, CHI).
- Integration with the hardware prefetcher.
