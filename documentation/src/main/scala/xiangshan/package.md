# package.scala

The package object for the `xiangshan` namespace.

## Overview
Contains global constants, helper functions, and implicit conversions that are available throughout the `xiangshan` package. It often includes common bit-width definitions and basic architectural constants.
